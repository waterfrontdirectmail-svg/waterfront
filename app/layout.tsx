import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Waterfront Direct Mail | Targeted Direct Mail for South Florida's Waterfront Homeowners",
  description: "Reach waterfront homeowners through hyper-targeted direct mail campaigns. We handle targeting, design, printing, and delivery for marine businesses across South Florida.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
